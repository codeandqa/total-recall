{
  "name": "total-recall-chrome",
  "version": "1.0.0",
  "private": true,
  "description": "Total Recall Chrome Extension - Unit Tests",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "test:verbose": "jest --coverage --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
